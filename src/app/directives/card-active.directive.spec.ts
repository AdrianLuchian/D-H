import { CardActiveDirective } from './card-active.directive';

describe('CardActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CardActiveDirective();
    expect(directive).toBeTruthy();
  });
});
